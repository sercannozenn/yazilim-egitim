<{{ $parentElement }} class="{{ $parentClass ?? '' }}">
<table class="{{ $class ?? '' }}">
    <thead>
        {!! $columns !!} }
    </thead>
    <tbody>
        {!! $rows !!} }
    </tbody>
</table>
</{{ $parentElement }}>
